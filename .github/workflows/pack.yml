name: Upload L10n Executables

on: 
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy-l10n:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - prefix: "EN"
            artifact_name: "l10n-en-pt"
          - prefix: "CHS"
            artifact_name: "l10n-chs-pt"
          - prefix: "CHT"
            artifact_name: "l10n-cht-pt"
          - prefix: "JA"
            artifact_name: "l10n-ja-pt"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: "Output/*${{ matrix.prefix }}*.exe"
          if-no-files-found: error
          retention-days: 45